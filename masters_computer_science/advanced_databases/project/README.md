# Blogging Platform Data Layer

Run the seed load and register the required functions with:

```
mongosh --shell "mongodb://localhost:27017/blog_project" blog_project.js
```

This script resets the `blog_project` database each time it runs.
