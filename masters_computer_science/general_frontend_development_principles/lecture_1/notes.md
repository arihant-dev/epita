# lecture 1

## Anatomy of a Browser

User Interface -> Browser Engine -> Rendering Engine -> JavaScript Engine -> Data Storage


## DOM (Document Object Model)
- The DOM is a programming interface for HTML and XML documents. It represents the page so that programs can change the document structure, style, and content. The DOM represents the document as a tree of nodes, where each node represents a part of the document (e.g., an element, attribute, or text).

rem - relative to the root element (html)